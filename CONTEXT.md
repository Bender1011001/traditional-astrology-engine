---
project: astrology
status: complete
updated: 2026-05-11
---

# Astrology Project

## Resume
- **Production auto-remediation drill exposed missing runner; bridge/workflow fix in progress (2026-05-11 UTC)**: Triggered a real Cloud Monitoring drill by writing an `ERROR` log to `cloud_run_revision` for `astrology-engine`; Google Monitoring fired and the live `gcloud-error-to-codex` bridge created GitHub issue `#8` (`production-error`, `codex`) at `2026-05-11T16:06:04Z`. The drill proved alert-to-GitHub works, but an `@codex` issue mention alone did not start any repair: no workflow ran, no PR opened, and issue `#8` remained open. It also proved the Cloud Monitoring webhook payload only included incident metadata/documentation, not the matched log payload/stack trace. Added `.github/workflows/codex-prod-error.yml` to run `openai/codex-action@v1` when an issue is labeled `production-error` or manually dispatched, install repo dependencies before Codex's networkless sandbox, enforce the production autofix risk gate, comment and label `needs-owner-review` when no concrete low-risk fix exists, run checks before PR creation, and only apply `autofix-safe` when `main` branch protection exists. The workflow only reacts to the `production-error` label event so separate non-production labels do not start or cancel the run. Added `OPENAI_API_KEY` as a GitHub repo secret. Hardened the bridge to query Cloud Logging for the newest matching recent incident log entry and include that payload in new issue bodies; the lookup uses a 5-minute default window and retries because fresh logs can lag indexing and Cloud Monitoring may reuse an old incident `started_at` while the incident remains open. Added `@google-cloud/logging` and focused tests. Local verification: bridge `npm test` -> `11 passed`, `node --check index.js` passed, workflow YAML lint passed, and a live Node `lookupMatchedLogEntry` call read the synthetic drill log (`CodexDrillError`) from Cloud Logging. Granted the bridge runtime service account `820685644947-compute@developer.gserviceaccount.com` `roles/logging.viewer` so deployed enrichment can read matching logs. Issue `#8` is intentionally synthetic and should be handled as no-code-change / owner-review, not as a real app bug.
- **Production error auto-remediation + GitHub Cloud Run deploy live (2026-05-11 UTC)**: Implemented and pushed the `C:\Users\admin\Downloads\production_error_autoremediation_runbook.md` flow. `gcloud-error-to-codex/` is a real Cloud Run Node/Express bridge that accepts Google Cloud Monitoring webhooks, validates `WEBHOOK_TOKEN`, normalizes alert payloads, dedupes by fingerprint, ensures GitHub labels, and creates or comments on Codex-ready GitHub issues in `Bender1011001/astrology`. GitHub workflows are live: `ci`, `risk_gate`, `automerge_safe`, `deploy_cloud_run`, and `deploy_error_bridge`; deploy workflows honor `[skip deploy]` in CI run titles except for manual dispatch. `scripts/deploy_cloudrun.py` supports `--skip-env-yaml` for GitHub Actions and suppresses Cloud Build log streaming. Created GitHub secret `GCP_SA_KEY` for `github-cloud-run-deployer@astrology-engine-prod.iam.gserviceaccount.com`; created BOM-free Secret Manager secrets `github-token` and `gcloud-error-webhook-token`; granted the deployer Cloud Run/Cloud Build/Artifact Registry/Secret Manager/IAM roles; granted the Cloud Run runtime service account access to the bridge secrets. GitHub Actions verified: latest `ci`, `deploy_cloud_run`, and `deploy_error_bridge` are green. Cloud Run readback: `astrology-engine-00099-hvs` at 100% traffic and `gcloud-error-to-codex-00004-lbw` at 100% traffic. Live checks: `https://traditional-astrology.com/api/healthz` returned healthy, bridge `/healthz/` returned OK, unauthorized bridge POST returned 401, and `severity>=ERROR` logs since `2026-05-11T15:00:00Z` returned `[]`. Real end-to-end bridge smoke created GitHub issue `#7` through the live GitHub API and it was immediately closed. Created Monitoring channel `projects/astrology-engine-prod/notificationChannels/13530373064463410887` and enabled alert policy `projects/astrology-engine-prod/alertPolicies/6488783140881813683` for `resource.type="cloud_run_revision" AND resource.labels.service_name="astrology-engine" AND severity>=ERROR` with 15-minute rate limiting and 1-day autoclose. Local verification before push: full suite `python -m pytest src\tests -q --tb=short` -> `594 passed`; `python -m compileall src`, all static JS `node --check`, bridge `npm test`, and `git diff --check` passed.
- **Production chart audit + Stripe webhook hotfix deployed (2026-05-11 UTC)**: Queried the real Google Cloud SQL PostgreSQL instance `astrology-engine-prod:us-central1:astrology-db` directly using the Secret Manager database URL and public IP `35.202.68.172` from the authorized local host. `chart_events` had `50` total rows and `19` completed free-instant chart generations in the rolling 48-hour window ending at DB time `2026-05-11T14:04:42Z`; exported the summary, per-event JSON, and saved reading HTML to `artifacts/chart-events-20260511/` with browseable `index.html`. Investigated Error Reporting `KeyError/AttributeError: get` from `stripe_webhook` on revision `astrology-engine-00095-nnv`; root cause was guest checkout webhook code calling `.get()` directly on Stripe SDK objects. Patched `src/api/v1/endpoints/billing.py` to use safe `_stripe_field` / `_stripe_mapping` access for session, metadata, and customer details, and added a regression test that uses a Stripe-like object without `.get()`. Verification: `python -m pytest src\tests\test_purchase_flow.py -q --tb=short` -> `8 passed`; `python -m compileall src\api\v1\endpoints\billing.py src\tests\test_purchase_flow.py` passed; `git diff --check -- src\api\v1\endpoints\billing.py src\tests\test_purchase_flow.py` returned only CRLF warnings. Deployed with `python scripts\deploy_cloudrun.py`; Cloud Build `c55becb8-1cfa-452a-b8ed-073829d1c584` pushed image digest `sha256:fb2a00328e4f137c9648dd7cfd99f40de72d8931889d631c60bf09703c57ab5a`; Cloud Run revision `astrology-engine-00097-89x` is serving 100% traffic. Live checks: `https://traditional-astrology.com/api/healthz` returned 200 and new-revision `severity>=ERROR` logs returned `[]`. Payment impact check: Stripe had one live `checkout.session.completed` event in the inspected May 10 window, a `$5` `horary_question` session with `payment_status=paid`; production DB had the matching `async_report_tasks` row marked `completed`, so that payment and fulfillment succeeded despite webhook retries failing. Also disabled stale duplicate Stripe webhook endpoint `we_1TUH2CC8BJritqvrbo0eCHaa` (`https://astrology-engine-820685644947.us-central1.run.app/api/v1/billing/webhook`) because it used a different signing secret and returned `400`; the canonical enabled endpoint is `https://traditional-astrology.com/api/v1/billing/webhook`.
- **Geomancy + Horary Oracle Unlimited deployed (2026-05-11 UTC)**: Verified and shipped the geomancy explainer/tool plus `$5/month` horary subscription model to production. Pre-deploy checks: full suite `python -m pytest src\tests -q --tb=short` -> `593 passed`; `python -m compileall src` passed; `node --check` passed for the changed horary/auth/geomancy/config/reading/chart/service-worker scripts; `git diff --check` returned only CRLF warnings. Tightened `.gcloudignore` before deploy so Cloud Build uploads the intended app payload while excluding `src/.env`, `src/.env.*`, pytest caches, bytecode, artifacts, `.claude`, local data, and the stray `ERROR` file. Deployed with `python scripts\deploy_cloudrun.py`; Cloud Build `45c3da68-ce8d-4686-990a-d64fe173a5e5` pushed image digest `sha256:7dc54b691e5ab56951c2f70d38b3e29fc1fd4b1090c25aa365b693fdb4c0d626`; Cloud Run revision `astrology-engine-00096-564` is serving 100% traffic. Live checks passed on both `https://traditional-astrology.com` and the direct Cloud Run URL: health 200, `/horary.html` has `$5/month` and no `$1`, `/geomancy.html` has strict-rules and Historical Use Only copy, `sw.js` has `astro-v22-geomancy-horary-subscription`, `js/horary-app.js` uses subscription endpoints and not the retired one-off checkout endpoint, unauthenticated valid `/api/v1/horary` casts return `401 Account sign-in is required`, `/api/v1/geomancy/cast` returns a judge, a live horary subscription Checkout Session can be created for a throwaway account and was immediately expired in Stripe, and new-revision Cloud Logging `severity>=ERROR` returned `[]`.
- **Geomancy launch positioning + Horary Oracle Unlimited built locally (2026-05-11 UTC)**: Acting from COO/product strategy, moved geomancy from a bare tool into an explanatory acquisition page and moved horary from the temporary per-question Stripe test SKU to a `$5/month` account subscription. `src/static/geomancy.html` now explains what geomancy is, why it fits a traditional astrology site, the strict odd/even shield rules, daughter extraction, parity combination, judge validity, shield positions, and geomancy-vs-horary positioning before/around the free cast UI. Horary is now deterministic-code monetization: `GET /api/v1/horary/access`, `POST /api/v1/horary/subscription/checkout`, and `POST /api/v1/horary/subscriber-answer` in `src/api/v1/endpoints/horary.py`; the old direct `/api/v1/horary` path is now subscription-gated too, preventing a free bypass. `src/services/db_seed.py` seeds the `horary` plan; `src/core/config.py` adds `STRIPE_PRICE_HORARY_MONTHLY`; `src/api/v1/endpoints/billing.py` verifies subscription checkouts and returns purchase metadata for GA4. `src/static/js/horary-app.js` handles account auth, subscription checkout, return verification, and subscriber casts. `src/static/dashboard.html` shows subscription renewal status and cancel-renewal controls. Homepage, terms, refunds, sitemap, service worker, tests, and context were updated. Local verification: focused tests `26 passed`; `python -m compileall src` passed; `node --check` passed for `js/horary-app.js`, `js/auth.js`, and `js/geomancy-app.js`.
- **Geomancy shield system + $1 horary price implemented locally (2026-05-10 UTC)**: Built a first-party classical geomancy path from `C:\Users\admin\Downloads\deep-research-report.md`. New engine `src/engine/geomancy.py` implements the report's procedural core: four mothers from odd/even line counts, daughters from mother rows, nieces/witnesses/judge/outcome by parity combination, 16-position Arabic house order, judge evenness validation, safety notice, source-basis metadata, and conventional Latin figure display labels with a caveat that source-specific semantic glosses still need direct manuscript extraction. Added `POST /api/v1/geomancy/cast` in `src/api/v1/endpoints/geomancy.py`, schema support in `src/api/v1/schemas.py`, and router registration. Added public `/geomancy.html` plus `src/static/js/geomancy-app.js`, shield/dot CSS, homepage/nav/footer/sitemap/service-worker links, and local API-base handling so FastAPI-served localhost pages use their own origin instead of forcing port 8000. Changed Horary Oracle from `$5` to `$1`: backend Stripe price validation/creation now expects 100 cents, frontend/fallback purchase value is 1, and horary/terms/refunds/home copy is updated. Verification: `python -m pytest src\tests\test_geomancy.py src\tests\test_purchase_flow.py src\tests\test_seo_acquisition_pages.py -q --tb=short` -> `25 passed`; `python -m compileall src` passed; `node --check` passed for `src/static/config.js`, `src/static/js/geomancy-app.js`, and `src/static/js/horary-app.js`; `git diff --check` reported only existing CRLF warnings; local FastAPI smoke on `http://127.0.0.1:8765/geomancy.html` returned health/page/API 200; Playwright desktop and mobile casts rendered 16 shield cards / 80 dot rows, no horizontal overflow, no console errors, screenshots in `artifacts/geomancy-smoke-20260510/`.
- **One free best reading per IP + traditional chart wheel deployed (2026-05-09 UTC)**: Changed the free premium endpoint so each real visitor IP still gets only one free LLM entitlement, but that entitlement now generates the best customer-facing tier: `premium_audit` / Complete Analysis with `report_iterations=3`. The quota key remains `free_premium_trial`, preserving existing one-per-IP history instead of granting repeat freebies. Updated public copy in `src/static/js/reading-app.js` from the weaker free-premium wording to "free Complete Analysis" / "one free best-tier LLM report". Replaced the old simple chart drawing in `src/static/js/chart-graphics.js` with a traditional astrology SVG wheel: zodiac glyph ring, sign boundaries, 5-degree ticks, Roman house numbers, ASC/DSC/MC/IC axes, planet glyph labels with degrees/signs/retrograde markers, and major aspect lines. Added matching wheel CSS and bumped the static cache contract to `rev20260509bestwheel1` / service worker `astro-v20-best-reading-wheel` with `/js/chart-graphics.js` precached. Verification: `node --check` passed for `chart-graphics.js`, `reading-app.js`, and `sw.js`; `python -m compileall src` passed; focused tests `python -m pytest src\tests\test_premium_report_quality.py src\tests\test_seo_acquisition_pages.py -q --tb=short` -> `25 passed`; full suite `python -m pytest src\tests -q --tb=short` -> `588 passed`. Local Playwright render check against representative chart data produced 12 zodiac glyphs, 60 degree ticks, 12 house numbers, 4 angle labels, 13 aspect lines, 12 planet labels, no console/page errors, and no 390px mobile horizontal overflow. Deployed Cloud Run revision `astrology-engine-00095-nnv` at 100% traffic. Live checks passed on `https://traditional-astrology.com`: health 200, homepage and natal page have `rev20260509bestwheel1`, `/sw.js` has `astro-v20-best-reading-wheel` and precaches `/js/chart-graphics.js`, `/js/chart-graphics.js` contains the traditional wheel assets and no old `TERRA` label, `/js/reading-app.js` has the new free Complete Analysis copy, and new-revision `severity>=ERROR` logs returned `[]`.
- **GA4 purchase conversion setup deployed (2026-05-09 UTC)**: Implemented the revenue-tag path on the live B2C funnel. `src/api/v1/endpoints/guest_checkout.py` now returns normalized GA4 ecommerce `purchase` metadata from verified Stripe sessions for `$25` full readings and `$69` complete analyses; `src/api/v1/endpoints/horary.py` does the same for the `$5` Horary Oracle checkout. `src/static/js/config.js` now has a deduped `trackPurchase()` helper keyed by Stripe `transaction_id`, and `reading-app.js` / `horary-app.js` call it after Stripe payment verification. Static cache contracts were bumped to `rev20260509ga4purchase1`, `20260509ga4purchase1`, and service worker `astro-v19-ga4-purchase` so returning visitors receive the new analytics code. `scripts/daily_funnel_report.py` now references current GA4 stream `G-5T7HPNKL7V`. Verification before deploy: `python -m pytest src\tests -q --tb=short` -> `587 passed`; focused purchase/tag tests -> `19 passed`; JS syntax checks passed for `config.js`, `reading-app.js`, `horary-app.js`, and `sw.js`; `compileall src` passed. Deployed Cloud Run revision `astrology-engine-00094-9hg` at 100% traffic. Live custom-domain checks passed: `/api/healthz` 200, homepage and `/horary.html` have current GA4 tag only, no old `G-RCNDWN4XVN` and no `GTM-TJNM5XVD`, new purchase JS cache-busters are present, `/sw.js` has `astro-v19-ga4-purchase`, `/js/config.js` contains the purchase event/dedupe helper, and new-revision `severity>=ERROR` logs returned `[]`. GA Admin readback shows `purchase` already exists as a key event on property `522604231`; attempting to create `free_chart_success` as a secondary key event failed with `ACCESS_TOKEN_SCOPE_INSUFFICIENT`, and `list_google_ads_links` returned `[]`, so Google Ads import still requires linking a Google Ads account and/or reauth with Analytics edit scope.
- **Postiz Codex skill added (2026-05-09 UTC)**: Added a reusable local Codex skill at `C:\Users\admin\.codex\skills\postiz` for Postiz social scheduling, media upload, integration discovery, post management, missing release-ID resolution, and analytics. Installed the global npm CLI with `npm install -g postiz`; `postiz --version` reports `2.0.13`. The skill keeps the operational workflow in `SKILL.md`, detailed platform/CLI examples in `references\postiz-cli.md`, and UI metadata in `agents\openai.yaml`. It requires real Postiz authentication (`POSTIZ_API_KEY` or `postiz auth:login`) before live account operations and reminds agents to preserve this project's Historical Use Only / no medical or financial advice guardrails in public social copy.
- **Post-deploy git reconciliation and funnel smoke (2026-05-09 UTC)**: Committed and pushed the full deployed tree to `origin/main` so production no longer lives only in a dirty local workspace. Commit: `a66cad0` (`Ship launch monitoring and account updates`). Staged only deploy-relevant code/config/docs (`.gcloudignore`, `Dockerfile`, `check_prod_db.py`, `scripts/`, `src/`, `CONTEXT.md`); left local `.claude` worktrees, `ERROR`, `artifacts/`, and local `data/**` dumps untracked. Pre-commit checks: staged secret-pattern scan found no Stripe/API/DB private values; `git diff --cached --check` passed after mechanically trimming trailing spaces from bundled `src/ephe/sefstars.txt`; focused regression `python -m pytest src\tests\test_stars.py src\tests\test_seo_acquisition_pages.py -q --tb=short` -> `39 passed`. Live funnel smoke on `https://traditional-astrology.com` used reserved test IP `203.0.113.210`: free chart generated a real completed `chart_event_id`; free-premium trial path completed or reached its existing per-IP safe state before checkout cleanup; account register/login/overview/save-chart path passed; guest full-reading Checkout Session and paid horary Checkout Session were created then explicitly expired through Stripe with `status=expired` and `payment_status=unpaid` for both. New revision Cloud Logging `severity>=ERROR` remained `[]`. Created Codex automation `astrology-production-watch`, hourly until `2026-05-10T16:00Z`, to check Cloud Run traffic, health endpoints, homepage GA/GTM state, and last-hour ERROR logs.
- **Full production deploy (2026-05-09 UTC)**: Shipped the full current local tree to Google Cloud Run after readiness checks. Pre-deploy verification: `python -m pytest src\tests -q --tb=short` -> `585 passed`; `python -m compileall src` passed; JS syntax checks passed for `consent.js`, `basic.js`, `script.js`, `js/auth.js`, `js/reading-app.js`, `js/horary-app.js`, `js/config.js`, and `js/landing.js`; `git diff --check` returned only line-ending warnings. `.gcloudignore` limits Cloud Build input to `Dockerfile`, `requirements.txt`, `LICENSE`, and `src/**`, so local artifacts, `.env`, archived data, and other untracked workspace files were not uploaded. Deployment command: `python scripts\deploy_cloudrun.py`. Cloud Build `cad707e4-5c63-4edd-a90f-868e3bd30bdb` succeeded and pushed image digest `sha256:1d13682bc400eec2e360e100111f9342b77ba9890906360d252cd8632d6f1955`. Cloud Run revision `astrology-engine-00092-zwm` is serving 100% traffic. Live checks passed on `https://traditional-astrology.com`: `/api/healthz` 200, `/` 200 with GA4 only and no GTM/old GA, `/account.html` 200 no-store/noindex and untracked, `/dashboard.html` 200 no-store/noindex and untracked, `/compatibility.html` 200 GA4 only, `/api/v1/charts/daily-horoscopes?target_date=2026-05-09` 200 with historical-use disclaimer, `/owner.html` 410 no-store/noindex, `/login.html` 302 to `/account.html?auth=login`, `/reset-password.html?token=abc%20123` 302 to `/account.html?auth=reset&token=abc%20123`, unauthenticated `/api/v1/account/overview` 401, `/blog.html` GA4 only and no GTM/old GA, invalid `/api/v1/horary/checkout` returns validation 422 without creating checkout, and `/api/v1/synastry` returned 200 for a real two-chart payload. New-revision Cloud Logging `severity>=ERROR` query returned `[]`.
- **Google Analytics MCP install/test (2026-05-09 UTC)**: Installed Google's official local Google Analytics MCP package, `analytics-mcp==0.4.0`, into `C:\Users\admin\.codex\mcp-venvs\analytics-mcp`. Enabled `analyticsadmin.googleapis.com` and `analyticsdata.googleapis.com` on `astrology-engine-prod`. ADC is now present at `C:\Users\admin\AppData\Roaming\gcloud\application_default_credentials.json` from the project-owned OAuth client `C:\Users\admin\.codex\analytics-mcp-oauth-client.json`; `gcloud auth application-default print-access-token` returns a token. The official package's ADK/stdio entrypoint hung on tool calls in this Windows stack, so Codex now uses `C:\Users\admin\.codex\analytics-mcp-stdio.py`, a small stdio MCP wrapper around the installed official `analytics_mcp.tools.*` functions. Global Codex config `[mcp_servers.analytics-mcp]` runs `C:\Users\admin\.codex\mcp-venvs\analytics-mcp\Scripts\python.exe C:\Users\admin\.codex\analytics-mcp-stdio.py` with `GOOGLE_PROJECT_ID=astrology-engine-prod` and `GOOGLE_APPLICATION_CREDENTIALS` pinned to the ADC file. Verified through MCP stdio: `get_account_summaries` returns account `andrew darcy` with properties `522604231 traditional-astrology.com`, `522626788 astrology`, and `525538780 FBA-bench`; `run_report` on property `522604231` for `7daysAgo..today` by `eventName/eventCount` returned `page_view=390`, `session_start=227`, `first_visit=217`, `a1=101`, and `form_start=45`. This server is read-only and cannot edit GA4 key events.
- **GA4 bot-noise cleanup (2026-05-09 UTC)**: Investigated the suspicious `390` GA4 number. It is `screenPageViews`, not people; same 7-day window showed `activeUsers=129`, `sessions=143`, and `eventCount=1047`. The page/path breakdown strongly indicates crawler/prober noise: `/login.html` had `89` active users and `166` views, `/owner.html` had `26` active users and `47` views, mostly direct desktop traffic across Chrome/Linux, Safari/Macintosh, Firefox/Windows, Firefox/Linux, Edge/Linux, Opera/Windows, and YaBrowser/Windows. Realtime had `0` active users. Current Analytics MCP is read-only, so it cannot edit hosted Google tags/GTM/GA admin settings. App-side mitigation was patched locally: auth legacy redirects now go to the untracked `/account.html?auth=...` page instead of tracked `/?auth=...`, reset-password redirects to `/account.html?auth=reset`, auth fallback history uses `/account.html?auth=login`, and `/owner.html` now returns `410 Gone` with no redirect plus no-store/noindex headers. Verification: `python -m pytest src\tests\test_api_integration.py -q --tb=short` -> `32 passed`; `python -m compileall src\app.py` passed; `node --check src\static\js\auth.js` passed. Not deployed yet because the worktree contains many unrelated pending changes.
- **Single Google Analytics tag cleanup (2026-05-09 UTC)**: Standardized the public site on one analytics implementation path: direct GA4 Google tag `G-5T7HPNKL7V`. Removed the remaining `GTM-TJNM5XVD` Tag Manager snippets and noscript iframes from `blog.html` plus the three blog article pages, removed the stale `G-RCNDWN4XVN` Google tag from the reset-password email template, and updated the privacy page / consent comment to describe Google Analytics only. Added `test_site_uses_one_google_analytics_tag_path()` so GTM and the old GA ID cannot drift back into static HTML or the reset email template. Verification: `rg -n "GTM-TJNM5XVD|googletagmanager\.com/gtm\.js|googletagmanager\.com/ns\.html|G-RCNDWN4XVN|Tag Manager" src\static src\templates src\tests -S` returned no matches; `python -m pytest src\tests\test_seo_acquisition_pages.py src\tests\test_api_integration.py -q --tb=short` -> `43 passed`; `node --check src\static\consent.js` passed. Not deployed yet because the worktree still contains many unrelated pending changes.
- **Owner key exposure remediation (2026-05-09 UTC)**: Treated the public `X-Owner-Key` in `check_via_api.py` as an active credential leak. Rotated `astrology-owner-bootstrap-key` in Google Secret Manager, updated Cloud Run revision `astrology-engine-00089-9tz` first to force new secret loading, then deployed revision `astrology-engine-00091-4s5` at 100% traffic with `PUBLIC_API_DOCS_ENABLED=false`. `check_via_api.py` now requires `OWNER_BOOTSTRAP_KEY` from the environment instead of a hardcoded key, catches specific urllib/JSON failures, and the current public GitHub `main` copy no longer contains an `OWNER_KEY = "..."` literal. `src/app.py` now disables `/docs`, `/redoc`, and `/openapi.json` unless `PUBLIC_API_DOCS_ENABLED` is explicitly true, with `src/core/config.py` defaulting it to false. Verification: old leaked key now returns `401`, latest Secret Manager key returns `200 {"ok":true}`, unauthenticated `/api/v1/owner/verify` and `/api/v1/owner/chart-events?limit=1` return `401`, `/docs` and `/openapi.json` return `404`, `/owner.html` still redirects to `/#get-reading`, `python -m pytest src\tests\test_api_integration.py -q --tb=short` passed with `31 passed`, and commit `f11fc59` was pushed to `origin/main`. Residual risk: GitHub history still contains the rotated key, so keep it considered burned.
- **Print/PDF export cleanup (2026-05-09 UTC)**: Added a dedicated print mode for generated chart readings so browser/PDF export no longer includes website chrome. `src/static/js/reading-app.js` now toggles `body.printing-reading` for `window.print()` and `beforeprint`/`afterprint`, allowing print CSS to focus on `#readingSection`. `src/static/style.css` now has a print stylesheet with `@page` zero margins, white paper styling, hidden nav/cookie/PWA/CTA/feedback/email/FAQ/footer-link UI, footer legal text retained, trace accordions expanded for print, page-break controls for reading blocks, and a constrained natal chart wheel. Cache busting moved the chart pages to `rev20260509print1`, and `src/static/sw.js` moved to `astro-v18-print-pdf`. Verification: `node --check src\static\js\reading-app.js` passed; `git diff --check -- src\static\style.css src\static\js\reading-app.js src\static\index.html src\static\natal-charts.html src\static\sw.js` reported only line-ending warnings; local Playwright on `http://127.0.0.1:8765/` submitted the 1996-08-13 07:18 Fairfield, CA chart, emulated print media, exported `artifacts/print-pdf-fix-20260509-090917/free-chart-print.pdf`, and PDF text extraction confirmed 3 pages with no source URL, browser date, browser pagination, cookie preferences, feedback, FAQ, or footer nav links while retaining the legal disclaimer.
- **Cheap paid horary Stripe test product (2026-05-09 UTC)**: Pivoted from the initial token idea to a simpler one-time `$5` Horary Oracle checkout because the goal is a cheap real Stripe test. `src/api/v1/endpoints/horary.py` now has `POST /api/v1/horary/checkout` to create a Stripe Checkout Session for one focused horary question and `POST /api/v1/horary/paid-answer?session_id=...` to verify payment, calculate the Regiomontanus horary answer, persist the completed result in `AsyncReportTask`, and record `GuestRequest(request_type="paid_horary_question")`. The legacy free `/api/v1/horary` route remains with the 5/month IP limiter for compatibility, but the public `src/static/horary.html` form now starts the paid checkout flow and renders the paid answer on return. `STRIPE_PRICE_HORARY_QUESTION` can pin an explicit Stripe Price; otherwise the endpoint searches/creates an active one-time `$5` USD Stripe Price with `tier=horary_question`. `src/static/sw.js` is bumped to `astro-v17-paid-horary` so returning visitors do not keep the old horary JS. Local verification: `python -m pytest src\tests\test_purchase_flow.py src\tests\test_horary_api.py -q` -> `7 passed`; `node --check src\static\js\horary-app.js` passed; local `POST /api/v1/horary/checkout` invalid payload returns the new route's `400 City is required for horary`; in-app browser on `http://127.0.0.1:8765/horary.html` shows the `$5` Stripe CTA with no console warnings. Local Uvicorn on port `8765` was restarted to load the new API route. Not deployed yet.
- **Mobile website optimization (2026-05-08 UTC)**: Tightened the public mobile funnel without changing backend behavior. `src/static/style.css` now has a more resilient mobile header/menu layout, safer tiny-screen brand/CTA sizing, better touch-target/input sizing, mobile wrapping for hero copy, trust items, buttons, Daily Navigator target-date controls, and compatibility secondary CTAs. `src/static/consent.js` now installs a real capture-phase mobile nav controller for pages that previously had inconsistent inline hamburger handlers; this fixed `/compatibility.html`, where the toggle set `aria-expanded=true` but did not show `.nav-links`. Static CSS cache busting moved to `rev20260508mobile1`, and `src/static/sw.js` moved to `astro-v15` / `astro-runtime-v15` with `consent.js` in the app shell so returning mobile visitors receive the menu fix. Added a tiny inline `mobile-nav-fallback` style beside static page stylesheet links so stale service-worker caches cannot keep the hamburger menu hidden while the fresh HTML is already loaded. Validation passed locally at 320px, 390px, and 768px for `/`, `/compatibility.html`, `/daily.html`, `/account.html`, and `/natal-charts.html`: status 200, no horizontal scroll, hamburger visible, menu opens, and no console warnings/errors.
- **Public My Account page fix (2026-05-08 UTC)**: Fixed the still-visible live account gap by adding a real `/account.html` customer entry page instead of relying on a hidden dashboard-only nav link. `src/static/account.html` now gives logged-out visitors a stable account page with Sign In, Create Account, forgot-password access, and clear dashboard handoff; signed-in visitors see an Open Dashboard action. `src/static/js/auth.js` now always exposes "My Account" in the public nav, routes logged-out users to `/account.html`, routes signed-in users to `/dashboard.html`, and lets account-page auth actions open the shared modal. Static HTML nav across the site was updated from hidden dashboard links to visible `/account.html` links, auth cache busting moved to `astro-v8`, and `src/static/sw.js` moved to `astro-v13` so old service-worker assets are replaced. `src/app.py` now serves `/account.html` with private/noindex cache headers and redirects `/account` plus `/account/` with temporary `302`s. Regression coverage in `src/tests/test_api_integration.py` verifies the public account page, nav link visibility, cache headers, and account redirects. Verification: focused account tests passed (`5 passed`), full `test_api_integration.py` passed (`27 passed`), local Playwright passed for homepage nav -> account page -> login/register modal -> signed-in dashboard state -> mobile menu, and live Playwright passed on `https://traditional-astrology.com` with no console errors. Deployed Cloud Run revision `astrology-engine-00084-dtk`, 100% traffic; live `/api/healthz`, `/`, and `/account.html` checks passed, and new-revision Cloud Logging showed no `ERROR` entries.
- **Account workspace + compatibility calculator build (2026-05-08 UTC)**: Added the next customer-facing product layer for a more complete astrology site. `src/api/v1/endpoints/account.py` now exposes authenticated account workspace APIs for overview, saved charts, chart deletion, report archive, report detail, and saved-chart transits backed by the existing `User.charts_saved` JSON and `AsyncReportTask` report rows. `src/static/dashboard.html` now has a real account workspace: save a chart, see saved charts, load a personal transit calendar from the Daily Navigator engine, and open completed free premium or paid reports linked to the account/checkout email. Report generation and guest checkout calls now send the current bearer token when signed in, and `src/api/v1/endpoints/premium.py` plus `src/api/v1/endpoints/guest_checkout.py` attach `user_id`/`account_email` metadata so generated reports can appear in the account archive. Added public `/compatibility.html` with a traditional synastry form and nav links across the static site; fixed `/api/v1/synastry` to call `calculate_chart_data()` with keyword arguments so `house_system="W"` is not misread as latitude. Regression coverage in `src/tests/test_api_integration.py` now exercises the account workspace endpoints and a real `/api/v1/synastry` POST. Verification: focused API tests `python -m pytest src\tests\test_api_integration.py -q` -> `23 passed`; full suite `python -m pytest src\tests -q` -> `573 passed`; local Playwright smoke passed for compatibility form submission, authenticated dashboard load, saved-chart transit generation, and report archive viewing. Deployed Cloud Run revision `astrology-engine-00082-2v6`, 100% traffic. Live custom-domain checks passed for `/api/healthz`, `/compatibility.html`, `/dashboard.html`, `/api/v1/synastry`, and unauthenticated `/api/v1/account/overview` returning 401; new-revision Cloud Logging showed no `ERROR` entries.
- **Real account page + daily horoscopes product pass (2026-05-08 UTC)**: Built the customer-facing account surface into a real private dashboard instead of a thin placeholder. `src/static/dashboard.html` now has account summary stats, profile/account details, saved-chart rendering from `/api/v1/auth/me`, reset-password/logout actions, and a saved Daily Horoscope Profile stored in the browser for quick personal briefings. Added a computed public daily horoscope product: `src/services/daily_horoscope.py` calculates seven traditional planet positions from Swiss Ephemeris/Moshier fallback and generates all twelve sign forecasts from real transits, sign rulers, planetary day, lunar phase, and whole-sign topics; `src/api/v1/endpoints/daily.py` exposes it at `GET /api/v1/charts/daily-horoscopes`. `src/static/daily.html` now leads with public Daily Horoscopes and then the personal Daily Navigator form, and the static-site nav label was standardized from "Daily Planets" to "Daily Horoscopes". Regression coverage was added in `src/tests/test_daily_navigator.py` and `src/tests/test_api_integration.py`. Verification: `python -m pytest src\tests\test_daily_navigator.py src\tests\test_api_integration.py -q` -> `32 passed`; local Playwright smoke confirmed `/daily.html` loads API-backed horoscope text, sign tabs switch, and `/dashboard.html` does not redirect to `#get-reading`; full suite `python -m pytest src\tests -q` -> `570 passed`. Deployed Cloud Run revision `astrology-engine-00081-kdx`, 100% traffic; live custom-domain checks passed for `/api/healthz`, `/api/v1/charts/daily-horoscopes?target_date=2026-05-08`, `/daily.html`, and `/dashboard.html`; production Playwright smoke confirmed the daily horoscope UI renders and dashboard no longer routes to `#get-reading`; new-revision Cloud Logging had no `ERROR` entries.
- **Flash Lite production switch + one free premium report per visitor IP (2026-05-08 UTC)**: Switched production LLM report generation to `google/gemini-3.1-flash-lite` after the real comparison run showed it was good enough and cheaper. Implemented the requested funnel shape: the normal free chart still renders instantly, then the frontend starts one free LLM premium report for a real visitor IP; additional premium reports go through paid Stripe checkout. Backend changes are in `src/api/v1/endpoints/premium.py` and `src/services/premium_generator.py`; frontend copy/CTA changes are in `src/static/js/reading-app.js`, `src/static/index.html`, `src/static/natal-charts.html`, and `src/static/sw.js`. Added regression coverage in `src/tests/test_premium_report_quality.py`; verification before deploy was `python -m pytest src\tests -q` -> `567 passed`, plus Black, JS syntax, and diff whitespace checks. Live free-premium smoke created task `584a1aa8-e7ee-4a9e-bffa-cb467b0667d4`, completed in about 80s, produced a clean 7,790-character customer report with no raw/technical appendix, and correctly blocked a second request from the same IP; artifacts are in `artifacts/live-free-premium-smoke-20260508-013510/`. Stripe production is live-mode only, so test-card payment cannot be completed without separate test-mode keys. Safe live checkout smoke created and immediately expired live Checkout Sessions for `$25` and `$69`; after correcting both deployed Stripe price IDs, final artifact `artifacts/stripe-live-checkout-smoke-final-verified-20260508-014929/checkout_smoke.json` confirms both tiers use active products, expected live price IDs, expected amounts, and expired status. Deployed final Cloud Run revision `astrology-engine-00080-xvj`, 100% traffic; live health returned `200`, homepage uses `rev20260508freepremium1`, service worker is `astro-v12`, and new-revision Cloud Logging had no ERROR logs and no Stripe fallback price warnings.
- **Premium report quality + Gemini Flash Lite comparison (2026-05-08 UTC)**: Fixed the paid-report failure mode where the customer output could still expose internal report scaffolding. `src/scripts/generate_premium_report.py` now uses ASCII-safe console prints so completed LLM runs do not fail on Windows after spending model calls, removes stale `$300/$197` prompt framing, and tells the model to use prose/simple bullets instead of Mermaid/code-fence markup. `src/services/premium_generator.py` strips the entire trailing `## Technical Appendix` section, not just the raw appendix marker, and `src/tests/test_premium_report_quality.py` asserts no internal appendix heading survives in customer output. Verification: `python -m pytest src\tests\test_purchase_flow.py src\tests\test_premium_report_quality.py -q` -> `14 passed`; `python -m pytest src\tests -q` -> `565 passed`; deployed Cloud Run revision `astrology-engine-00077-qxb`, 100% traffic; live `GET /api/healthz` returned `200`, and new-revision ERROR logs were empty. Model test artifact: `artifacts/model-comparison-promptfix-20260508-005227/` contains the computed chart data, sanitized current-model baseline from `premium_reports/andrew_darcy_premium_20260420_212801.md`, and a real six-pass `google/gemini-3.1-flash-lite` top-tier report for 1996-08-13 07:18 Fairfield, CA. Flash Lite generated in 44.71s, produced 6,213 customer-facing words after cleanup, passed the same core chart-fact checks as the 7,078-word baseline, emitted no Mermaid/code fences or stale price text, and OpenRouter listed it at exactly half the per-token prompt/completion price of `google/gemini-3-flash-preview`. Production model was not changed yet.
- **Dashboard route + Google MCP setup (2026-05-08 UTC)**: Fixed the sticky account redirect problem after earlier revisions had permanently redirected `/dashboard.html` to `/#get-reading`. `src/app.py` now serves `/dashboard.html` through an explicit `FileResponse` before the static mount with `Cache-Control: no-store, no-cache, must-revalidate, max-age=0`, `Pragma: no-cache`, `Expires: 0`, and `X-Robots-Tag: noindex, nofollow`; `/dashboard` and `/dashboard/` now use temporary `302` redirects to `/dashboard.html` instead of cacheable `301`s. Regression coverage in `src/tests/test_api_integration.py` asserts the dashboard page is a `200` account surface with no `Location` header and that short dashboard paths redirect temporarily. Verification: `python -m pytest src\tests -q` -> `555 passed`; deployed Cloud Run revision `astrology-engine-00074-php`, 100% traffic; live custom-domain checks confirmed `/dashboard.html` is `200` with the private cache headers and `/dashboard` plus `/dashboard/` are `302 /dashboard.html`. If a browser still jumps to `/#get-reading`, clear site data for `traditional-astrology.com` or use incognito because the old `301` can be cached client-side. Also installed useful Google Cloud MCP servers globally in Codex: `gcp-resource-manager`, `gcp-cloud-run`, `gcp-cloud-sql`, `gcp-cloud-logging`, `gcp-cloud-monitoring`, `gcp-error-reporting`, `gcp-cloud-asset`, `gcp-cloud-storage`, and `gcp-compute-engine`. They run through `C:\Users\admin\.codex\google-mcp-remote.ps1`, which fetches a fresh `gcloud auth print-access-token` at server startup and proxies to the Google remote MCP endpoint with `mcp-remote`, avoiding stored bearer tokens. Active gcloud project was corrected to `astrology-engine-prod`, and `roles/mcp.toolUser` was granted to the active Google user on that project.
- **Swiss Ephemeris fixed-star catalog production fix (2026-05-08 UTC)**: Resolved the high-volume Error Reporting issue `swisseph.fixstar: SwissEph file 'sefstars.txt' not found in PATH '/usr/share/swisseph:/usr/local/share/swisseph/'`. Added bundled `src/ephe/sefstars.txt` with source/hash documentation, set `SE_EPHE_PATH=/app/src/ephe:/usr/share/swisseph:/usr/local/share/swisseph` in the Dockerfile, configured Swiss Ephemeris to prefer the packaged ephemeris path, and added a one-time logged fallback in `src/engine/stars.py` so missing catalog files do not spam production. Added fixed-star regression coverage in `src/tests/test_stars.py`. Verification before the dashboard deploy: focused star tests passed, full suite passed, Cloud Run revision `astrology-engine-00073-2gp` served 100% traffic, live free-reading and Stripe checkout smoke passed, and new-revision logs had zero matches for `sefstars.txt`, Stripe `InvalidHeader`, or inactive product errors.
- **Customer-readiness production pass (2026-05-08 UTC)**: Standardized the public B2C offer around one-time guest purchases: free instant preview, `$25` Full Natal Chart Reading, and `$69` Complete Astrological Analysis. Deprecated practitioner/B2B acquisition pages now redirect to `/#get-reading`, sitemap/robots were updated, terms/refunds/FAQ no longer promise subscriptions, trials, or practitioner APIs, and checkout now rejects inactive/stale Stripe prices before creating a live session. Mobile consent UI was tightened so it no longer covers paid CTAs, and `sw.js` now uses network-first document handling with `astro-v10`/`astro-runtime-v10` so returning visitors do not stay pinned to stale launch pages. Sensitive Cloud Run values are mounted through Secret Manager, `.gcloudignore` now allowlists the deploy payload, and Cloud Build uploads only 564 files / 7.7 MiB. Stripe webhook fulfillment now recognizes the live guest tiers (`full_reading`, `premium_audit`) so a paid order still creates a report task if the customer closes the browser before the return-page script runs. Deployed Cloud Run revision `astrology-engine-00071-t4j`, 100% traffic. Verification: `550 passed` locally, then live custom-domain smoke passed on `https://traditional-astrology.com`: health, homepage `$25` pricing/no `$49`, service worker v10, retired-page redirects, free reading generation with persisted `chart_event_id`, owner readback, Stripe Checkout session creation, smoke session expiration, and zero error logs for the new revision. Browser screenshots from the readiness pass are in `artifacts/readiness-final/`. Remaining admin cleanup: remove old GA destination forwarding for `G-RCNDWN4XVN` in Google Tag/Analytics if only `G-5T7HPNKL7V` should receive events. Security action: rotate production secrets because earlier diagnostic commands exposed live secret values in terminal output before the Secret Manager cleanup.
- **Auth persistence + one-project Cloud SQL cutover (2026-05-08 UTC)**: Root cause of account loss was Cloud Run revision `astrology-engine-00061-4xh` running without `DATABASE_URL`, so successful registrations were written to in-container SQLite while Cloud SQL still showed 0 users. Rotated the Cloud SQL `postgres` password, enabled Secret Manager in `astrology-engine-prod`, created `astrology-database-url`, and changed `src/database/core.py` to fail fast on Cloud Run if `DATABASE_URL` is missing. Added real forgot/reset modal support in `src/static/js/auth.js`, `/forgot-password.html` and `/reset-password.html?token=...` redirects in `src/app.py`, reset timestamp normalization in `src/engine/user_auth.py`, regression tests, and static asset cache bumps to `rev20260508authdb1` / `astro-v7`. Migrated Cloud SQL data from `astrology-487423:us-central1:astrology-db` into new in-project `astrology-engine-prod:us-central1:astrology-db`, updated `scripts/deploy_cloudrun.py` to use `--set-cloudsql-instances` for the in-project instance, and deployed Cloud Run revision `astrology-engine-00064-k42` serving 100% traffic. Live verification: homepage/dashboard use new assets, `/forgot-password.html` redirects to `/?auth=forgot`, `/reset-password.html?token=abc123` redirects to `/?auth=reset&token=abc123`, registration/login/me work, reset-password works, target DB has 2 smoke users/2 subscriptions/4 plans, and the old DB did not receive the post-cutover smoke registration.
- **Google Cloud project cleanup status (2026-05-08 UTC)**: Correct production project is now `astrology-engine-prod`; it owns Cloud Run, the `traditional-astrology.com` domain mapping, Secret Manager, and the active `astrology-db` Cloud SQL instance. Old `astrology-487423` still contains the pre-cutover Cloud SQL instance and an old `astrology-engine` Cloud Run service; keep it only as a short rollback copy until explicitly deleted. `polymarket-489301` was explicitly approved for deletion after inspection showed a running Compute Engine VM named `tail-risk-engine` in `europe-west4-a` with a 20 GB disk. Normal deletion was blocked by App Hub `MANAGEMENT_PROJECT_DELETE`; the App Hub boundary was detached with `gcloud apphub boundary update --project=polymarket-489301 --location=global --crm-node=""`, then `gcloud projects delete polymarket-489301 --quiet` succeeded. `gcloud projects describe polymarket-489301` now reports `lifecycleState: DELETE_REQUESTED`; it can be temporarily restored with `gcloud projects undelete polymarket-489301` during Google's recovery window.
- **Account nav/live fix (2026-05-07)**: Fixed the live "My Account" flow that was sending account intent back to `/#get-reading`. Root causes were the global CSS rule hard-hiding `#navAccountBtn` even after `auth.js` removed `.hidden`, and stale auth-page redirects that sent `/login.html` to the chart funnel. `src/static/style.css` now lets `.hidden` control account visibility; `src/static/js/auth.js` opens the real login/register modal for `?auth=required`, `?auth=expired`, `?auth=login`, and `?auth=register`, then returns auth-intent logins to `/dashboard.html`; `src/app.py` redirects legacy auth URLs to modal intents instead of `/#get-reading`. Static CSS/auth JS query strings were bumped across pages to `rev20260507account1` and `astro-v6` to bypass the 86400s asset cache. Deployed to Cloud Run revision `astrology-engine-00061-4xh`, serving 100% traffic. Live checks on `https://traditional-astrology.com/` confirmed the new asset versions, `/login.html` returns `302 /?auth=login`, `/dashboard.html` returns `200` with dashboard HTML, `X-Robots-Tag: noindex, nofollow`, and `Cache-Control: no-store`.
- **Free-chart/revenue emergency fix (2026-05-01)**: Root cause found in production logs: after Cloud SQL persistence was fixed, every `/api/v1/premium/guest/request` was being keyed as Cloud Run's internal link-local client `169.254.169.126`, so the "3 free readings per IP" gate became a global service-wide lockout and returned 402 after three tests. Added `src/api/v1/client_ip.py` to prefer `X-Forwarded-For`/real visitor headers, changed free-reading quota to a rolling visitor window, record `GuestRequest` only after successful chart generation, and skip quota enforcement for non-rate-limitable fallback proxy addresses. Live smoke test on `https://traditional-astrology.com/api/v1/premium/guest/request` returned `200 completed`, `instant=True`, ~13k chars HTML, and Cloud Logging now shows a public visitor IP instead of `169.254.169.126`.
- **Revenue/checkout evidence (2026-05-01)**: Cloud Run logs since 2026-04-01 show only two successful `/api/v1/guest/checkout` session-creation requests and no paid generation calls. Stripe live API check for the last 45 days showed 100 recent Checkout Sessions returned by the API page, 0 paid sessions, 0 PaymentIntents, 0 succeeded payments, and $0.00 received. This means the technical checkout endpoint can create sessions, but no one has completed payment.
- **Conversion cleanup (2026-05-01)**: Landing page now removes Sign In/Create Account nav/modal cues from the main B2C path because current model is guest-only checkout, not accounts. Prices are aligned to `$25` full reading and `$69` complete analysis. `reading-app.js` now emits GA4 funnel events: `free_chart_submit`, `free_chart_success`, `free_chart_paywall`, `free_chart_error`, `checkout_click`, `checkout_redirect`, `checkout_error`, `paid_return`, `paid_generation_started`, and `paid_generation_error`. A top-of-result conversion bar was added above every successful free chart so the paid CTA appears immediately after chart generation, not only at the bottom of a long reading. Static asset query strings were bumped to `rev20260501funnel2` because JS/CSS cache for 86400 seconds. Global CSS hides legacy `navLoginBtn`/`navAccountBtn` on older SEO pages until those pages are rebuilt; live `natal-charts.html` still contains old login markup but CSS hides it. Live checkout smoke test created a Stripe Checkout Session through `/api/v1/guest/checkout` and then expired the test session successfully.
- **Latest investigation and fix (2026-05-01)**: GA4 and GSC mismatch is expected because GSC reports Google Search clicks/impressions while GA4 reports JS-tracked page activity. Cloud Run logs showed real scanner/prober noise (`wp-login.php`, `.env`, `.git/config`, `xmlrpc.php`) returning mostly 404/405. `/dashboard.html` and `/login.html` traffic in the Apr 3-Apr 30 GA window was not owner usage; available Cloud Run request logs show crawler/prober user agents including YandexBot, Bingbot, Amazonbot, AhrefsBot, Bytespider, Meta external agent, ClaudeBot, Stripebot, OpenAI search bot, GoogleOther, BuiltWith, and PowerShell checks. GA/GTM is now removed from `src/static/dashboard.html`; private/API surfaces emit `X-Robots-Tag: noindex, nofollow`; `/dashboard.html` is now included in the legacy auth-page 301 redirects to `/#get-reading` so it cannot continue serving as a tracked/private page.
- **Analytics cleanup/reporting (2026-05-01)**: Removed duplicate Google Tag Manager from static HTML and kept direct GA4 `G-RCNDWN4XVN` so frontend `gtag("event", ...)` funnel events still work without possible GTM double-counting. Live checks on `/`, `/natal-charts.html`, and `/sect-in-astrology.html` confirmed `hasGtag=True` and `hasGTM=False`; `/dashboard.html` and `/login.html` confirmed `301 /#get-reading`, `hasGtag=False`, `hasGTM=False`. Added `scripts/daily_funnel_report.py` to report production funnel health from Cloud Run logs plus Stripe live API instead of noisy GA snapshots. Command used: `python scripts\daily_funnel_report.py --date 2026-05-01 --days 1 --limit 2000`. Latest output: 38 browser-like page views, 4 unique browser-like IPs, 6 free chart successes, 11 free chart failures, 1 checkout session created, 0 paid sessions, $0.00 gross. GA Admin API attempt to mark key events for property `522626788` failed with `403 ACCESS_TOKEN_SCOPE_INSUFFICIENT`; key events still need Analytics Admin UI access or a token with `analytics.edit`.
- **Acquisition/SEO sprint (2026-05-02 UTC)**: Added three buyer-intent static pages targeting free chart and conversion searches: `free-natal-chart-pdf.html`, `astrology-reading-for-clients.html`, and `sect-astrology-calculator.html`. Each page is indexable, direct-GA4-only, contains Historical Use / no medical/legal/financial advice language, and links above the fold to `/#get-reading`. Homepage now has a "Popular Free Calculators" internal-link section pointing to the new PDF preview, sect calculator, and annual profections page; footer links now emphasize those free acquisition paths rather than old B2B pages. `reading-app.js` now tracks `print_save_pdf` and uses a real `printReading()` handler; the free result conversion bar includes a `Print / Save Preview PDF` action to support the "free natal chart PDF" search intent truthfully. Static asset query strings bumped to `rev20260501seo1`. Added `src/tests/test_seo_acquisition_pages.py` plus a `.gitignore` exception so new `src/tests/test_*.py` regression tests are trackable.
- **Chart event persistence and feedback (2026-05-02)**: Added durable `chart_events` and `reading_feedback_events` SQLAlchemy tables so future free chart generations save the submitted birth inputs, request context, generated chart summary, full returned reading HTML, reading hash, remaining free count, and generation timing. The free chart API now returns `chart_event_id`/`reading_hash`; the B2C result UI sends a real Good/Bad vote tied to that chart event and the backend persists it, while preserving older up/down feedback compatibility. Owner-only inspection is available at `/api/v1/owner/chart-events?limit=50` with the configured `X-Owner-Key`; pass `include_reading_html=true` only when the full HTML is needed. Static asset query strings bumped to `rev20260502chartvotes1`. Regression added in `src/tests/test_chart_event_tracking.py`. Live smoke produced chart event `f58c84dd-f21e-4d13-b249-2a7dc4b9de90` with London 1990-01-01 12:00, `sect=DAY`, 13,069 chars of HTML, and one persisted `good` feedback vote.
- **Auth/account evidence (2026-05-01)**: Google Cloud SQL exists as `astrology-487423:us-central1:astrology-db`. Direct SQL counts after production redeploy showed zero `users`, zero `user_subscriptions`, zero `invoices`, zero `api_keys`, zero `usage_records`, zero `guest_requests`, zero `async_report_tasks`, zero `horary_rate_limits`, zero `leads`, zero `outreach_targets`, zero `outreach_attempts`, and zero user-created chart/delineation rows; only the four seeded `subscription_plans` rows exist. Cloud logs available back to 2026-04-01 showed zero successful `POST /api/v1/auth/register`, zero successful `POST /api/v1/auth/login`, zero `/api/v1/auth/me`, and only the investigation owner API check returning 401. Public chart-like usage did occur before the fix: 87 successful POST 200 requests across `/api/v1/premium/guest/request`, `/api/v1/horary`, and `/api/v1/charts/daily-briefing` in the available log window.
- **Last deployment (2026-05-03 UTC)**: Live Cloud Run service `astrology-engine` in `astrology-engine-prod/us-central1` is deployed at revision `astrology-engine-00045-xw4`, serving 100% traffic, with `DATABASE_URL` present, `OWNER_BOOTSTRAP_KEY` configured, and Cloud SQL annotation `astrology-487423:us-central1:astrology-db`. Startup creates new tables via `Base.metadata.create_all`. Live checks confirmed `/` returns `200`, uses `rev20260502chartvotes1`, no longer references `rev20260501seo1`, has `gtag=True`, and `GTM=False`. Owner readback through `/api/v1/owner/chart-events` confirmed the live smoke chart event and Good vote persisted. The production service account has `roles/cloudsql.client` on the DB project, and `sqladmin.googleapis.com` is enabled in the production project. `scripts/deploy_cloudrun.py` includes the Cloud SQL attachment, `--quiet`, and disables local gcloud file logging so env values are not written to SDK logs.
- **Secret hygiene note (2026-05-01)**: Local gcloud log files from this and older deploy work contained parsed env var values because `gcloud run deploy --env-vars-file` logs arguments. Matching local gcloud logs were removed, a follow-up scan found no remaining env markers, and `core/disable_file_logging` was set for gcloud. Rotate the live DB/API/SMTP/Stripe secrets if this machine or transcript is considered exposed.
- **Pick up at**: Site fully **LIVE and SECURE** on Google Cloud Run (`https://traditional-astrology.com`). DNS records successfully point to Google's IPs and Google trust certificates are verified. Targeted tests green for visitor-IP extraction/rate limiting.
- **Last session**: Validated DNS propagation and SSL certificate issuance. Deployed Phase 2 Continuous Improvements:
  1. **Static Asset Edge Caching**: Added `CacheControlMiddleware` (`max-age=86400` for assets, `300` for HTML) to improve Lighthouse/Web Vitals.
  2. **Graceful Error Parsing**: Updated `reading-app.js` and `script.js` to map and join FastAPI 422 array responses instead of displaying `[object Object]` alerts.
  3. **Cloud Logging Observability**: Upgraded `ActivityLogger` to auto-detect `K_SERVICE` env var and swap to `JSONFormatter` on `sys.stdout` for native ingestion into Google Cloud Logging.
  4. **UI Loading Feedback**: Fixed `submitBtn` so `.btn-loading` span automatically toggles visibility and hides `.btn-text` when calling the API, fixing stagnant button UI.
- **Blocked on**: GA4 key-event marking still needs Analytics Admin UI access or OAuth scope `https://www.googleapis.com/auth/analytics.edit`; local gcloud auth currently lacks that scope. Deleting old `astrology-487423` should wait until the new in-project database has been stable long enough or the rollback copy is no longer needed. The B2C pipeline, deployment, custom domains, monitoring, and PWA systems are operational.

## Status
- **Working**: Astrological engine core, B2C consumer reading site, guest checkout (Stripe one-time payments), account workspace/report archive, daily horoscopes, compatibility calculator, Computation Trace ("Show Our Work") integrated.
- **Broken**: None known in the customer purchase path. Admin cleanup remains for old GA destination forwarding and post-exposure secret rotation.
- **Business Model (B2C)**: Direct-to-consumer natal chart readings remain guest-checkout first. Horary is the exception: account-based `$5/month` recurring access because it is a low-cost deterministic question engine and recurring access must be tied to a billing identity.
- **Free**: Instant template-based readings per visitor rolling window (no LLM, ~3-5s) plus one free best-tier Complete Analysis LLM report per real visitor IP.
  - **$25**: Full natal chart reading (LLM-generated, one-time Stripe payment)
  - **$69**: Premium in-depth analysis (LLM-generated, one-time Stripe payment)
  - **$5/month**: Horary Oracle Unlimited (deterministic code, no LLM verdict generation)
- **Free Tool**: Classical geomancy explainer and shield chart at `/geomancy.html`, backed by the report-derived `/api/v1/geomancy/cast` endpoint.
- **Database**: Comprehensive pre-1700s traditional astrology—Managed via `AstrologicalDelineation` database table (SQLAlchemy).
- **Release Ready**: Version 2.0 (B2C Consumer Readings).
- **SEO**: Content pages (natal-charts, houses, aspects, etc.) kept for organic traffic with CTA banners → reading form.


## Tech Stack

- Python 3.10+
- FastAPI, Uvicorn
- Swiss Ephemeris (`pyswisseph`)
- Vanilla JS, CSS (Glassmorphism)
- Google Gemini Flash (Generative AI)
- ReportLab (PDF Generation)
- Geopy, TimezoneFinder, pytz (Location & Time)
- Stripe (Payments)
- PyJWT (Authentication)


## Deployment (Google Cloud Run)

- **Platform**: Google Cloud Run (managed, serverless containers)
- **Project**: `astrology-engine-prod`
- **Region**: `us-central1`
- **Service URL**: `https://astrology-engine-jknswoor2a-uc.a.run.app`
- **Latest verified revision**: `astrology-engine-00096-564` (100% traffic on 2026-05-11 UTC)
- **Custom Domain**: `traditional-astrology.com` (mapped and live)
- **Resources**: 512Mi memory, 1 CPU, 0-3 instances, 300s timeout
- **Build**: `gcloud builds submit` with kaniko caching (72hr TTL)
- **Deploy**: `python scripts/deploy_cloudrun.py` or manual `gcloud run deploy --env-vars-file env.yaml`
- **Entry Point**: `uvicorn src.app:app` (defined in `Dockerfile`)
- **Static Files**: Served by FastAPI via `src/app.py` mount (Single Service Architecture)
- **Health Check**: `GET /api/healthz` → `{"status": "healthy", "version": "2.0.0"}`
- **Custom 404**: `src/static/404.html` (auto-served by Starlette StaticFiles)
- **Database**: Cloud SQL PostgreSQL `astrology-engine-prod:us-central1:astrology-db`; Cloud Run reads `DATABASE_URL` plus sensitive API/payment/auth/email secrets from Secret Manager.

## AI Configuration
- **Model**: `google/gemini-3.1-flash-lite` (via OpenRouter)
- **Reproducibility**: Enforced via `temperature=0.2` and `top_p=0.85` for plain readings.
- **System Prompts**: Strict adherence to chart data and deterministic output structure.



## Database Inventory (SQLAlchemy: `astrological_delineations` table)

The core delineations are now stored in the database to allow for manual fixes and persistent overrides. The `src/database/data/legacy/` directory contains the original JSON files.

### Natal Chart Delineations
- `planets_in_signs.json` — 168 entries (7 planets × 12 signs × 2 sects)
- `planets_in_houses.json` — 84 entries (7 planets × 12 houses)
- `detailed_delineations.json` — General planet/sign descriptions

### Essential Dignities
- `terms_bounds.json` — Egyptian Terms for all 12 signs
- `faces_decans.json` — 36 Faces/Decans with Chaldean rulers
- `triplicities.json` — Fire/Earth/Air/Water Day/Night rulers

### Predictive Techniques
- `firdaria.json` — Firdaria planetary periods (Day/Night chart orders)
- `profections.json` — Annual Profection house meanings (ages 0-95)
- `solar_return_moon_houses.json` — SR Moon in natal houses

### Fixed Stars & Lunar Mansions
- `fixed_stars.json` — 16 major stars (4 Royals, Behenian, violent)
- `lunar_mansions.json` — 28 tropical mansions with electional properties

### Lots & Arabic Parts
- `lots_arabic_parts.json` — 16 Lots (Fortune, Spirit, Eros, Marriage, etc.)

### Longevity & Health
- `hyleg_alcocoden.json` — Vitality/longevity technique with planetary years
- `medical_astrology.json` — Iatromathematics (body parts, humors, critical days)

### Aspects & Eclipses
- `aspect_delineations.json` — 5 Ptolemaic aspects with sect variations
- `aspect_natures.json` — Basic aspect natures
- `eclipse_rules.json` — Solar/Lunar eclipse interpretation

### Electional Astrology
- `electional_considerations.json` — Moon conditions, planetary hours, Bonatti rules

### Outreach & Leads
- `Lead` — Inbound marketing lead capture from website.
- `OutreachTarget` — Curated list of people/shops for outbound contact.
- `OutreachAttempt` — Logs of what was sent/attempted and when.

### Other
- `house_topoi.json` — House significations
- `example_charts.json` — Historical charts (Lilly, Newton, Napoleon, etc.)
- `glossary.json` — Traditional astrological terms

## Security & Audit Status
- **Verified**: '-d' city suffix backdoor removed from `charts.py` (2026-02-07).
- **Verified**: Admin and Owner endpoints require environment-based secret keys.
- **Verified**: Stripe webhook signature verification active.

## Documentation Reference
- `docs/SYSTEM_ARCHITECTURE.md` — High-level system overview.
- `docs/SECURITY_DOCUMENTATION.md` — Security model and remediation history.
- `docs/API_GUIDE.md` — v1 Endpoints and payloads.
- `docs/engine/architecture.md` — Deep dive into the Auditor and Service layer.
- `docs/engine/auditor.md` — Rules for the forensic audit.
- `docs/DEPLOYMENT.md` — VPS setup, env vars, and updates.
- `docs/FRONTEND.md` — Client-side architecture.
- `docs/TESTING.md` — Test suite guide.

## Key Files
- `src/app.py` — Entry point for the web server (Documentation at /docs).
- `src/static/js/reading-app.js` — B2C reading flow (form → free/paid → poll → render). Includes `buildTraceSection()` for rendering computation trace.
- `src/engine/trace_generator.py` — Reusable trace module: generates JSON dict of all computation steps for any chart.
- `src/services/free_reading_generator.py` — **Instant free reading**: Template-based, zero-LLM reading generator. Extracts Sun/Moon/Rising, sect, dignity scorecard, profections from `Auditor` data and renders consumer-friendly HTML.
- `src/services/premium_generator.py` — Background task for LLM-generated premium reports (paid tiers only); includes computation trace generation (step 3).
- `src/api/v1/endpoints/premium.py` — Free guest reading endpoint (instant) + paid reading polling endpoint.
- `src/api/v1/endpoints/guest_checkout.py` — Guest Stripe checkout (no auth, $25/$69 one-time purchases).
- `src/api/v1/endpoints/geomancy.py` — Classical geomancy shield cast endpoint from report-derived parity rules.
- `src/engine/geomancy.py` — First-party geomancy shield engine: mothers, daughters, nieces, witnesses, judge, outcome.
- `src/api/v1/endpoints/account.py` — Authenticated account workspace API for saved charts, transits, and report archive.
- `src/static/dashboard.html` — Private account workspace UI.
- `src/static/compatibility.html` — Public traditional synastry / compatibility calculator.
- `src/static/geomancy.html` / `src/static/js/geomancy-app.js` — Public geomancy shield chart surface.
- `LICENSE` — MIT License.
- `CHANGELOG.md` — Project history and rule updates.
- `src/database/data/glossary.json` — Astrological definitions.
- `src/engine/chart_calculator.py` — Core astronomical logic.
- `src/engine/calculate_advanced_mechanics.py` — Core math for electional/horary logic
- `src/engine/forensic_engine.py` — Forensic audit hub (replaced logic.py).
- `src/engine/classical_mechanics.py` — Antiscia, Dodecatemoria, Planetary Hours engine.
- `src/engine/dignities.py` — Essential dignity engine based on "Missing Codex".
- `src/engine/pdf_generator.py` — PDF report generation engine.
- `src/database/db_manager.py` — Loader for Codex delineations.
- `src/engine/chat_oracle.py` — RAG interface for chart Q&A.
- `src/engine/user_auth.py` — User authentication & accounts module.
- `scripts/outreach_run.py` — Automated outreach runner (email-only).
- `scripts/extract_all_traditional_data.py` — Comprehensive data extraction.
- `scripts/migrate_json_to_db.py` — Migrates JSON data to the SQLAlchemy database.
- `src/engine/daily_navigator.py` — Daily Navigator engine: synthesizes all timing layers into one briefing.
- `src/api/v1/endpoints/daily.py` — API endpoint for daily prediction briefings.
- `src/static/daily.html` — Frontend page for the Daily Navigator.

## Data Sources
- `Binder1.txt` — Combined traditional source material (Valens, Firmicus, Lilly, Dorotheus).
- `docs/research/` — Lunar Mansions, Hyleg/Alcocoden, Placidus methods, Planetary Hours.

## Architecture Quirks
- Uses a custom "Egyptian Terms" system found in the binder which includes Sun and Moon.
- Implements "Universal Overdrive" logic where mundane events (eclipses) prioritize over natal placements.
- All planet-in-sign delineations have Day/Night sect variations.
- **User Authentication**: Uses PBKDF2 password hashing (100k iterations), JWT tokens for sessions, and **SQLAlchemy** (SQLite local / PostgreSQL prod).
- **Total Logging Architecture**: 
  - **Backend**: `RequestLoggingMiddleware` captures all requests/responses/timings.
  - **Frontend**: Telemetry system captures clicks/errors and sends them to `/api/log/telemetry`.
  - **Storage**: JSON-structured logs with rotation in `logs/astrology_engine.jsonl`.
- **SEO & Search Visibility**:
  - **Canonical Domains**: All variants (www, http) are redirected to `https://traditional-astrology.com` via `CanonicalDomainMiddleware` to prevent indexing fragmentation.
  - **Crawlability**: `robots.txt` explicitly allows public pages but protects `/api`, dashboard files, and auth flow files.
  - **Internal Network**: Guide pages include "Related Guides" sections to ensure deep crawlability for search agents.

## Trap Diary
| Issue | Cause | Fix |
|-------|-------|-----|
| Syntax Error in Logic | Improper use of multi_replace_file_content range | Full re-write of logic module |
| Missing South Node | Only North Node calculated by default | Added derived South Node calculation |
| 500 Server Error in Chart Calc | `swisseph` constant mismatch | Updated constant to `swe.ECL2HOR` |
| Negative Vitality Score | Hyleg calculation without floor | Added safety clamp (min 5 years) |
| Placeholder Delineations | Moon Pisces had "NOT FOUND" text | Enhanced via enhance_delineations.py |
| Cache Manager Crash | `datetime.timedelta` undefined (wrong import) | Fixed import to `from datetime import datetime, timedelta` |
| Double Rate Limit | Free users counted twice per request | Removed duplicate check before cache lookup |
| Email Fake Success | Dev mode returned `True` without sending | Returns `False` + logs warning when not configured |
| Hardcoded URLs | Base URL was hardcoded in email links | Added `SITE_BASE_URL` env var |
| CORS Inflexibility | Hardcoded CORS origins | Added `CORS_ORIGINS` env var with sensible defaults |
| Console.error in Prod | JS errors visible to users | Silenced non-critical console outputs |
| Bare `except:` clauses | Catches all exceptions including system exits | Changed to `except Exception:` for safer handling |
| Stub Login/Register | Pages showed "coming soon" | Implemented full auth system with user_auth.py |
| Missing Function NameError | `_log_event` called but not defined in `api.py` | Restored missing helper function |
| Azure DB Auth Failure (legacy, migrated to Cloud Run) | Used `astrology_admin` instead of `astroadmin` | Migrated to Google Cloud Run — no longer applicable |
| ACR ImagePullFailure (legacy, migrated to Cloud Run) | Truncated Registry Password used in App Settings | Migrated to Google Cloud Run — no longer applicable |
| Plan Not Found (Reg) | Database not seeded with standard plans | Added `seed_plans()` to app startup event |
| Decennials | Apheta / Sequence | Confirmed correct. Zodiacal sequence (`(p.lon - asc) % 360`) correctly computes post-ascendant alignment. |
| Stars | Parans & Hour Angles | Confirmed correct. Uses proper spherical astronomy (RA/Dec and Hour Angle offsets) for Asc/Desc/MC/IC intersection logic. |
| Zodiacal Releasing | L4 Duration Drift | `prediction.py` L4 duration hardcoded to `0.208`. Replaced with `(2.5 / 12.0)` to eliminate mathematical drift across extensive calculations. |
| Decumbiture | Critical Day Lunar Math | Confirmed correct. The Newton-Raphson implementation uses safely wrapped modulos handling up to 270 degrees perfectly without anti-particle trapping. |
| Solar Return | Muntha Exact Degree | Confirmed correct. Muntha accurately maps to `(Sign * 30) + (Asc % 30)` for high-precision overlays. |
| Indexing Blind Spot | Lack of canonicalization & authority signals | Added `CanonicalDomainMiddleware` and tuned `robots.txt` |
| Crash on startup | SyntaxError in `forensic.py` | Removed duplicate argument |
| CSP Violations on `/docs` | `SecurityHeadersMiddleware` blocking CDNs | Added `jsdelivr` and `tiangolo` to allow-list |
| SyntaxError: duplicate argument | Repeated `house_system` in method signature/calls | Cleansed signature and corrected API calls |
| High bounce rate | B2B SaaS pitch vs B2C search intent mismatch | Complete site redesign to consumer readings |
| Zero conversions | Account signup friction | Removed all auth; guest-only checkout flow |
| SEO CTR penalty | JS DOM mutation left B2B traces for Googlebot | Baked consumer CTA UI directly into HTML and deleted seo-bridge.js |
| Stripe CSP blocked | Checkout domain not in Content-Security-Policy | Added checkout.stripe.com + js.stripe.com to CSP |
| All free readings fail | `premium_generator.py` passed `house_system=` to `generate_chart_data()` which doesn't accept it | Removed the unsupported kwarg from the lambda call |
| Glossary tooltip text rendered inline, garbling all reading text | `basic.js` creates `.glossary-tooltip-popup` elements but `style.css` had NO CSS rules for them — popups showed as visible text | Added 56 lines of tooltip CSS: `display:none` by default, shown on hover/active |
| New static pages always redirect to index | `app.py` has `_LEGACY_REDIRECTS` list that 301-redirects listed pages BEFORE static file handler sees them | Remove the page from `_LEGACY_REDIRECTS` before it can be served as a static file |
| Primary directions trace silently empty | `raw["meta"]` uses `lat` not `latitude` for geographic latitude | Use `.get("lat")` not `.get("latitude")` |
| 31 pages not indexed in GSC, 5 indexed | `robots.txt` blocked `about.html` and `faq.html` despite them being in `sitemap.xml`, and correctly blocked B2B/auth pages | Removed `about.html` and `faq.html` from `robots.txt` disallow list |
| UnicodeEncodeError crashes pytest on Windows | Pytest captures `sys.stdout` but Windows default `cp1252` encoding crashes on emojis (🦅) output by `logger.py` | Added fallback `sys.stdout.reconfigure(encoding='utf-8', errors='replace')` to console handler in `logger.py` |
| Silent cache failures for charts | `json.dumps()` in `CacheManager` crashed on `PlanetName` Enum objects | Added `default=str` kwargs to `json.dumps()` |
| API integration tests failing on /calculate | Endpoint transitioned from returning legacy `astronomical` key to `astronomy` via `ForensicEngine` | Updated `test_api_integration.py` assertions to expect `astronomy` |
| Daily Navigator crashes on dict planets/houses | `calculate_chart_data` returns planets as `{name: data}` dict and houses as `{int: cusp}` dict, but `_rebuild_chart_model` assumed list format | Added dict-handling branches for both planets and houses in `_rebuild_chart_model` |
| Planet constructor rejects `sign` kwarg | `Planet.sign` is a computed `@property` from longitude, not a dataclass field | Removed `sign=` from `Planet()` constructor calls |
| Chart constructor rejects `house_cusps` kwarg | `Chart` model uses `houses: Optional[Dict[int, float]]`, not `house_cusps: List[float]` | Changed to pass `houses=` dict instead |
| CSP trailing space crashes h11 on Cloud Run | CSP header ending with `; ` (space after semicolon) causes `h11.LocalProtocolError: Illegal header value` on every request | Remove trailing space/semicolon from last CSP directive |
| Root-level routes shadowed by StaticFiles mount | `app.mount("/", StaticFiles(html=True))` catches ALL paths before `@app.get("/healthz")` — returns 404 for HTML file not found | Use `/api/healthz` prefix so API router handles it before static mount |
| `.gcloudignore` `scripts/` excludes `src/scripts/` | `scripts/` glob matches both root `scripts/` and `src/scripts/` — latter is needed by `premium_generator.py` import chain | Use `/scripts/` (root-only) in `.gcloudignore` |
| Cloud Run logs show as text | Standard python `logging` writes text lines to `sys.stdout` which Cloud Logging doesn't parse | Check for `os.environ.get("K_SERVICE")` and inject a custom `JSONFormatter` into `StreamHandler` |
| `[object Object]` on validation error | FastAPI 422 errors return `err.detail` as a list of dicts (`[{"loc": ..., "msg": ...}]`) which crashes `alert(err)` | Map the array to extract `{d.loc[x]}: {d.msg}` into a unified string |
| Array indexing `IndexError` at exactly 360.0 degree boundary | `int(lon / 30)` evaluates to `12` at exactly 360 | Wrapped mathematics with bounds `int((lon % 360.0) / 30.0) % 12` |
| Silent Combustion Check Failures in Electional.py | Engine checked for `"COMBUST"` but calculations.py explicitly maps Moon combustion to `"DARK_MOON"` | Corrected the strings in `electional.py` to match Lunar phase strings |
| Horary Semantic Perfection Physics Bugs | `horary.py` contained hardcoded positive offsets preventing retrograde/sinister interference detection and backwards logic for collection/translation weight validation | Re-wrote condition boundaries using the relative `dist = get_aspect_distance()` instead of basic algebra, and corrected logical `and`/`or` boundaries for weight checks |
| Essential Dignity Peregrine Exclusivity Bug | `dignities.py` lacked Peregrine scoring in the core calculator and improperly protected planets in Fall/Detriment from Peregrine compounding in the variant calculator | Implemented independent Peregrine stacking (-5) whenever `domicile==exaltation==triplicity==term==face==0` to match historic point values |
| Aspect Application Reversal Vector | `aspects.py` `is_applying` falsely flagged separated aspects as applying and applying aspects as separating due to mathematically inverted sign checks against the relative velocity derivative `d(p2-p1)/dt`. | Restructured distance-to-velocity gradient checks: logic now successfully demands that `rel_speed > 0` when `dist > 0`, and `rel_speed < 0` when `dist < 0`. |
| Primary Directions Placidus Pole Proxy Bug | `primary_directions.py` improperly hardcoded the geographic latitude (the Ascendant's pole) as the proxy Pole for all planet-to-planet Promittor projections. | Intercepted the calculation sequence to derive each Significator's exact proportioned Pole and Hemispheric quadrant via `_get_pole_and_hemisphere()`, enabling true "Under the Pole" Oblique progression. |
| Hyleg Baseline Disqualification Bug | `hyleg.py` disqualified any planet in the 1st House via a hardcoded `altitude > 0` filter, contradicting the Ascendant's status as a primary Hylegical place. | Bypassed the altitude filter specifically for House 1, acknowledging that the 1st house spans below the horizon natively. |
| Medical Astrology Missing Hostile Aspects | `medical.py` solely checked Conjunctions for Malefic/Luminary interference in the remediation window calculator, silently ignoring the classical Square/Opposition strictures. | Injected explicit mathematical geometric constraint validations for 90° (Square) and 180° (Opposition) with appropriate severity point scaling. |
| Equatorial Sect Math Corruption | `mundane.py` and `phasis.py` illegally passed Ecliptic planetary longitude coordinates to `swe.azalt` while using the `swe.EQU2HOR` or `swe.FLG_SWIEPH` projection flags. This grossly corrupted the apparent Solar altitude and caused fatal day/night sect inversions. | Re-mapped the projection matrices universally to `swe.ECL2HOR` to accurately parse physical ecliptic coordinates. |
| Void of Course Kinematic Intersection Bug | `calculations.py` `is_void_of_course` falsely flagged the Moon as VoC when the target planet was moving backwards or very fast, because it used a static sign boundary separator `dist_to_target < dist_to_end` rather than projecting the intersection dynamically over time `v = d/t`. | Rewrote the intersection condition to explicitly solve for the dynamic offset (`time = dist / closing_speed`, then `moon_travel = time * moon_speed`), ensuring bounds-checking occurs at the exact mathematical point of perfection. |
| Hermetic Lot Polarity Inversion | `lots.py` accidentally inverted the `a_lon` and `b_lon` arguments for the Lots of Necessity, Courage, and Nemesis during daytime calculations, calculating them backwards (e.g. `Asc + Mercury - Fortune` instead of `Asc + Fortune - Mercury`), contradicting Hellenistic physics because they relate to the Lot of Fortune rather than Spirit. | Mathematically swapped the arguments for the day/night polarity branching to faithfully reconstruct the Paulus Alexandrinus physics framework. |
| Dodecatemoria Harmonic Offset Corruption | `advanced_mechanics.py` superimposed its calculation arc against the planet's longitude (`longitude + arc`) rather than the start of the sign boundary (`sign_start + arc`), inadvertently boosting the formula by an entire N-harmonic factor (calculating x13 instead of Valens x12, and x14 instead of Paulus x13). | Repointed the mathematical projection vector to hinge entirely off `sign_start`, securing the intended classical geometric mapping to the micro-zodiac. |
| Humoral Temperament Tally Dilution | `temperament.py` indiscriminately added the inherent nature of *all* 7 traditional planets to the humor tally instead of just the *significant* planets (Asc ruler, Moon, and planets aspecting the Moon/Asc), creating a homogeneous 'blob' temperament for all nativities. It was also missing 60° (Sextile) and 120° (Trine) aspects in its aspect detectors. | Restructured the routine to first compile a `significant_planets` set, added checking for Sextile/Trine/Ascendant aspects, and restricted the inherent nature tally to only iterate over the authorized set matching Lilly's specific workflow. |
| Abscission False Collision & Hellenistic VoC Bug | `horary.py` `check_abscission` wrongly assumed that any physical interposition resulted in a collision cut, because it used `abs()` on the closing speed, failing to gate for when the interposing planet is moving away faster than the pursuer. Additionally, `check_void_of_course_hellenistic` judged the 30-degree boundary using static aspect distance rather than kinematic moon travel distance. | Swapped absolute speed comparison in Abscission to dynamic directional vector `closing_speed`, verifying true convergence over time. Implemented `v=d/t` kinematic time projection into the Hellenistic VoC checks. |
| Cloudflare analytics wildly different from GSC | Cloudflare is DNS-only (grey cloud), not proxying. DNS resolves to Google IPs (216.239.x.x), `server: Google Frontend`. CF dashboard shows DNS query counts (bots, resolvers), not page views. | Confirmed architecture is correct as Google Cloud Run direct. GA4 + GSC is the real analytics stack. Removed orphaned `cloudflared_config.yml` and `.cloudflared/`. |
| `www.traditional-astrology.com` times out | No DNS record configured for `www` subdomain in Cloudflare | Need to add CNAME record: `www` → `traditional-astrology.com` in Cloudflare DNS dashboard |
| Synastry form returns 400 | `/api/v1/synastry` passed `house_system` positionally into `calculate_chart_data()` after latitude/longitude were inserted before house options | Use keyword arguments for all chart options and cover with a real `/api/v1/synastry` POST regression test |

## Anti-Patterns (DO NOT)
- Do not edit planets_in_signs.json manually without running enhance_delineations.py
- Do not use Day/Night delineations interchangeably—sect matters
- Do not ignore sect when calculating triplicities or Firdaria
- Do not hardcode URLs—use `SITE_BASE_URL` environment variable
- Do not use bare `except:` clauses—always catch specific exceptions
- Do NOT add login/signup/account requirements to the reading flow — it kills conversions
- Do NOT add subscription/monthly pricing — one-time only for B2C
- Do NOT use technical jargon ("forensic audit", "Swiss Ephemeris") in consumer-facing copy

## Environment Variables (Production)
| Variable | Required | Description |
|----------|----------|-------------|
| `STRIPE_SECRET_KEY` | Yes | Stripe live secret key |
| `STRIPE_WEBHOOK_SECRET` | Yes | Stripe webhook signing secret |
| `JWT_SECRET` | Yes | Secret for signing JWT tokens (auto-generated on Render) |
| `OPENROUTER_API_KEY` | Yes | OpenRouter API key for AI readings |
| `SENDGRID_API_KEY` | Yes* | SendGrid API key for email (*or configure SMTP) |
| `SMTP_HOST` | Yes* | SMTP server host (*if not using SendGrid direct) |
| `SMTP_PORT` | No | SMTP server port (default: 587) |
| `SMTP_USER` | Yes* | SMTP username |
| `SMTP_PASS` | Yes* | SMTP password |
| `SENDER_EMAIL` | Yes | From address for outgoing emails |
| `OUTREACH_POSTAL_ADDRESS` | Yes* | Postal address for CAN-SPAM compliance footer (*required for sends) |
| `SITE_BASE_URL` | No | Base URL for links in emails (default: https://traditional-astrology.com) |
| `CORS_ORIGINS` | No | Comma-separated allowed origins (default: production + localhost) |

## Build / Verify
```bash
python src/tests/test_audit.py
uvicorn src.api:app --reload
python scripts/extract_all_traditional_data.py  # Regenerate data to JSON
python scripts/migrate_json_to_db.py           # Sync JSON data to Database
python scripts/enhance_delineations.py         # Refresh delineation quality
```
